ALGORITHM traverse_array
VAR
   tab1 : ARRAY_OF INTEGER[5];
   tab2 : ARRAY_OF INTEGER[6];
   i,j,sum:integer
BEGIN
  tab1 := {1,2,3,5,7,8};
  tab2 := {1,2,3,5,7,8};
sum:=0

for i=0 to length.arr1 i++ 
for j=0 to length.arr2 j++
   if (arr1[i]!=arr2[j]) then
      sum:=sum+arr1[i]
   END_IF
 END_FOR
END_FOR


 for i=0 to length.arr2 i++ 
 for j=0 to length.arr1 j++


if (arr2[i]!=arr1[j]) then
     sum:=sum+arr2[i];
END_IF
 END_FOR
END_FOR
 write(sum)

END